# Content Architecture — environment variables
# Only the variables this skill actually needs (see README + files/04-automation-spec.md).
# Manual-first skill (Automation 2/5): the architecture is a design judgement; no API keys.
# The validator (click-depth BFS + orphan scan + URL regex) runs locally on the documented sitemap.

PROJECT_ROOT=          # absolute path to the project root on this machine
