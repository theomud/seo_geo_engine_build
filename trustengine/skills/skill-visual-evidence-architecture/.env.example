# Visual Evidence Architecture — environment variables
# Only the variables this skill actually needs (see README + files/04-automation-spec.md).
# Manual-first skill (Automation 2/5): no paid API keys required. Screenshot capture is
# done by hand from the live official sources; infographic render-checks run locally.

PROJECT_ROOT=          # absolute path to the project root on this machine
